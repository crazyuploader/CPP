language: cpp
compiler: g++
script:
  - for f in $(ls *.cpp); do echo "Compiling $f"; g++ $f; done
  - for f in $(ls Practical_File/*.cpp); do echo "Compiling $f"; g++ $f; done
  - for f in $(ls programs/cpp/Practical_File/concat/*.cpp); do echo "Compiling $f"; g++ $f; done
